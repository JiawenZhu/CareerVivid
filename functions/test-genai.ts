import { GoogleGenAI } from "@google/genai";
async function test() {
  const ai = new GoogleGenAI({});
  try {
     // Just mock to see the response type from the typescript definitions
  } catch (e) {}
}
