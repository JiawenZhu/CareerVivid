# UI Kit — CareerVivid Chrome extension

Recreation of the extension side panel (`src/extension-ui/`): sticky blurred header with logo + 36px avatar, "Job detected" application workspace, match breakdown, action cards (16–20px radius, `#ececf4` borders on `#f8f8fb`), sticky bottom nav. Min height 520px, width ~380px.
