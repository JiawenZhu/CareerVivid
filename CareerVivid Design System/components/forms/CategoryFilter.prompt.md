Category filter pill row — the one place near-black (#030712) appears as a fill: the active pill. Inactive pills are white with gray border and purple-tint hover.

```jsx
<CategoryFilter options={['All', 'Technology', 'Finance', 'Creative']} value={cat} onChange={setCat} />
```
