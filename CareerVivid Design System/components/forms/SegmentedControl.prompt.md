Segmented control — #f3f4f6 track (4px padding, 8px radius), 34px white active item with purple text and a hairline shadow. Use for view toggles (Grid / Row) and mode switches.

```jsx
<SegmentedControl options={['Grid', 'Row']} value={view} onChange={setView} />
```
