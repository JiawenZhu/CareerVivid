Standard text field — white, 1px #d1d5db border, 8px radius, purple focus ring (#625bd5 + soft glow).

```jsx
<Input label="Company" placeholder="e.g. Figma" value={v} onChange={setV} />
```

Height 42–52px. Font 13px/600. For the big rounded search bar use SearchInput instead.
