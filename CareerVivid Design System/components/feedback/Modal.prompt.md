Modal dialog — 20px radius (spec 18–24), 20px/800 Jakarta heading, rgba(17,24,39,.45) backdrop, deep soft shadow.

```jsx
<Modal title="Delete this resume?" onClose={close} actions={<><Button variant="neutral">Cancel</Button><Button variant="danger">Delete</Button></>}>
  Name the failure and give the next action.
</Modal>
```
